# Increase network timeout to avoid network-related failures
network-timeout=60000

# Skip optional dependencies to reduce build complexity
optional=false

# Ensure consistent dependency resolution
legacy-peer-deps=true

# Disable fund messages during installation
fund=false
